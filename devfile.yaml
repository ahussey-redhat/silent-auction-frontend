---
schemaVersion: 2.2.0
metadata:
  name: silent-auction-demo-frontend
components:
  - name: nodejs
    container:
      image: 'quay.io/tstasse/silent-auction-demo-frontend:latest'
      mountSources: true
      sourceMapping: /projects
      memoryLimit: 5G
      endpoints:
        - exposure: public
          name: nodejs
          protocol: https
          targetPort: 8080
      command:
        - sleep
        - infinity
commands:
  - exec:
      commandLine: pnpm install && pnpm extract-messages && pnpm compile-messages
      component: nodejs
      label: Install
      workingDir: '${PROJECTS_ROOT}/silent-auction-demo-frontend'
    id: 1-install
  - exec:
      commandLine: pnpm dev
      component: nodejs
      group:
        kind: run
      label: Start dev server
      workingDir: '${PROJECTS_ROOT}/silent-auction-demo-frontend'
    id: 2-dev
